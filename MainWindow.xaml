<Window x:Class="KeithleyControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:tbex="clr-namespace:TextBoxEx"
        xmlns:btnex="clr-namespace:ButtonEx"
        xmlns:helpers="clr-namespace:KeithleyControl.Helpers"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="GENERATOR TEST" Height="500" Width="1400">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Background="#f8f9fc">
            <Grid.RowDefinitions>
                <RowDefinition Height="240" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="1*" />
            </Grid.RowDefinitions>
           <GroupBox Header="CONTROL" Grid.Row="0" Margin="5,0,5,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="1*" />
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="5,5,5,0">
                        <TextBlock Text="Interface" VerticalAlignment="Center" HorizontalAlignment="Left"></TextBlock>
                        <ComboBox x:Name="Interface"
                                  Text="{Binding SelectedInterface, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  SelectedItem="{Binding SelectedInterface}" Width="125" HorizontalAlignment="Right">
                            <ComboBoxItem Content="LAN"></ComboBoxItem>
                            <ComboBoxItem Content="TB-USB"></ComboBoxItem>
                            <ComboBoxItem Content="GPIB"></ComboBoxItem>
                        </ComboBox>
                    </Grid>
                    <Grid Grid.Row="1" Margin="5,5,5,0">
                        <TextBlock Text="IP:" VerticalAlignment="Center" HorizontalAlignment="Left"></TextBlock>
                        <TextBox Text="{Binding IpAddr, UpdateSourceTrigger=PropertyChanged}" Height="25" Width="125"
                                 HorizontalAlignment="Right">
                        </TextBox>
                    </Grid>
                    <Grid Grid.Row="2" Margin="5,5,5,0">
                        <TextBlock Text="Port:" VerticalAlignment="Center" HorizontalAlignment="Left"></TextBlock>
                        <tbex:TextBoxEx Width="125" Text="{Binding Port, UpdateSourceTrigger=PropertyChanged}" Step="1"
                                        Minimum="0" HorizontalAlignment="Right" />
                    </Grid>

                    <Grid Grid.Row="3" Margin="5,5,5,0">

                        <btnex:ButtonEx Content="Connect" Width="80" ButtonType="Normal" HorizontalAlignment="Left"
                                        IsEnabled="{Binding SocketModel.ConnectFlag}"
                                        Command="{Binding ConnectCommand}" />
                        <btnex:ButtonEx Content="Disconnect" Width="80" ButtonType="Normal" HorizontalAlignment="Right"
                                        IsEnabled="{Binding SocketModel.DisConnectFlag}"
                                        Command="{Binding DisconnectCommand}" />
                    </Grid>
                    <Grid Grid.Row="4" Height="25" Margin="5,5,5,0">
                        <!--<TextBlock Text="Command:" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="16,0,0,0"></TextBlock>
                        <TextBox Text="{Binding SocketModel.Command}" HorizontalAlignment="Center"></TextBox>-->

                        <TextBox Width="150" Text="{Binding SocketModel.Command}" HorizontalAlignment="Left">
                            <TextBox.Resources>
                                <VisualBrush x:Key="HintText" TileMode="None" Opacity="0.5" Stretch="None"
                                             AlignmentX="Left">
                                    <VisualBrush.Visual>
                                        <TextBlock FontStyle="Italic" Text="Command:" />
                                    </VisualBrush.Visual>
                                </VisualBrush>
                            </TextBox.Resources>
                            <TextBox.Style>
                                <Style TargetType="TextBox">
                                    <Style.Triggers>
                                        <Trigger Property="Text" Value="{x:Null}">
                                            <Setter Property="Background" Value="{StaticResource HintText}" />
                                        </Trigger>
                                        <Trigger Property="Text" Value="">
                                            <Setter Property="Background" Value="{StaticResource HintText}" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>
                        <btnex:ButtonEx Content="Send" Width="50" ButtonType="Normal" HorizontalAlignment="Right"
                                        IsEnabled="{Binding SocketModel.SendFlag}"
                                        Command="{Binding CmdDebugCommand}" />
                    </Grid>
                    <Grid Grid.Row="5" Margin="5,5,5,0">
                        <TextBox Width="218" Height="50" Text="{Binding SocketModel.Response}"
                                 HorizontalAlignment="Left" TextWrapping="WrapWithOverflow">
                            <TextBox.Resources>
                                <VisualBrush x:Key="HintText" TileMode="None" Opacity="0.5" Stretch="None"
                                             AlignmentX="Left">
                                    <VisualBrush.Visual>
                                        <TextBlock FontStyle="Italic" Text="Response...." />
                                    </VisualBrush.Visual>
                                </VisualBrush>
                            </TextBox.Resources>
                            <TextBox.Style>
                                <Style TargetType="TextBox">
                                    <Style.Triggers>
                                        <Trigger Property="Text" Value="{x:Null}">
                                            <Setter Property="Background" Value="{StaticResource HintText}" />
                                        </Trigger>
                                        <Trigger Property="Text" Value="">
                                            <Setter Property="Background" Value="{StaticResource HintText}" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>

                    </Grid>
                </Grid>
            </GroupBox>
            <Grid Grid.Row="2" VerticalAlignment="Bottom" Height="25" Background="#FFE0DCDC">
                <TextBlock Text="{Binding LogInfo}" VerticalAlignment="Center" Margin="5,0,0,0"></TextBlock>
            </Grid>
        </Grid>
        <Grid Grid.Column="1">
            <wv2:WebView2 helpers:WebBrowserHelper.BindableSource="{Binding WebBrowserSource}" x:Name="webView" />
        </Grid>
    </Grid>

</Window>